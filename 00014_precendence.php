<?php
    //multiplication have higher than >= 
    // && have higher priority than ||
    // & have higher priority than |
    $x = "Hello";
    $y = "world";
    echo $x.$y;
    echo "<br>";
    echo $x.=$y;
